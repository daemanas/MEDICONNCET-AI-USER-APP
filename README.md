# MediConnected

This workspace contains the **MediConnected AI User App** (React Native CLI).

Open:

**[MediConnectedAI/README.md](MediConnectedAI/README.md)**

The Provider/Admin web portal and the shared Express + MongoDB backend live in the sibling folder `provider_mng`.
